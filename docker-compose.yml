services:
  app:
    build: .
    container_name: php_app
    ports:
      - "8000:80"
    volumes:
      - ./srrc:/var/www/html
    depends_on:
      - db
  
  db:
    image: mysql:8.0
    container_name: mysql_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: houssam.123.321
      MYSQL_DATABASE: echoblog
      MYSQL_USER: user
      MYSQL_PASSWORD: houssam.123.321
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
